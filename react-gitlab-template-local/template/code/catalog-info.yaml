apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ name }}
  annotations:
    gitlab.com/project-slug: ${{ owner }}/${{ repo }}
    backstage.io/techdocs-ref: dir:.
spec:
  type: website
  lifecycle: experimental
  owner: ${{ owner }}
